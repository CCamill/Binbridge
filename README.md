# Binbridge

<p align="center">
  <b>二进制代码函数命名的多模态大语言模型</b>
</p>

Binbridge 是一个创新的深度学习系统，能够自动为二进制代码中的函数生成有意义的名称。它结合了滑动窗口编码、注意力查询桥接（Q-Former）和蒸馏式思维链技术，在保持高效推理的同时实现了专家级的命名质量。

## 🌟 特性

- **滑动窗口编码**：通过重叠策略处理任意长度的汇编序列
- **Q-Former 压缩**：将数千行汇编代码压缩为 64 个高密度语义向量
- **蒸馏式思维链**：利用 GPT-4 生成的分析作为训练信号
- **QLoRA 高效微调**：4-bit 量化 + 低秩适配器，大幅降低显存需求
- **端到端推理**：自动过滤分析过程，直接输出函数名

## 📁 项目结构

```
binbridge/
├── config.py              # 配置文件
├── train.py               # 训练脚本
├── inference.py           # 推理脚本
├── utils.py               # 工具函数
├── requirements.txt       # 依赖列表
├── model/
│   ├── __init__.py
│   ├── encoder.py         # 滑动窗口编码器
│   ├── qformer.py         # Q-Former 模块
│   └── binbridge.py       # 主模型
├── data/
│   ├── __init__.py
│   └── dataset.py         # 数据集处理
└── scripts/
    └── prepare_data.py    # 数据准备脚本
```

## 🚀 快速开始

### 安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/binbridge.git
cd binbridge

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# or
.\venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt
```

### 准备数据

数据格式为 JSON，每个样本包含：
```json
{
    "assembly": "push rbp\nmov rbp, rsp\n...",
    "function_name": "calculate_checksum",
    "arch": "x86_64",
    "opt": "O2",
    "analysis": "可选的思维链分析..."
}
```

生成示例数据：
```bash
python -c "from data.dataset import generate_sample_data; generate_sample_data('./data/train.json', 1000)"
```

### 训练

```bash
python train.py \
    --train_data ./data/train.json \
    --eval_data ./data/eval.json \
    --output_dir ./outputs \
    --epochs 10 \
    --batch_size 4 \
    --learning_rate 2e-4 \
    --use_cot
```

### 推理

```bash
# 批量推理
python inference.py \
    --model_path ./outputs/best_model \
    --input ./data/test.json \
    --output ./results.json \
    --batch_size 8

# 交互模式
python inference.py \
    --model_path ./outputs/best_model \
    --interactive
```

## 🏗️ 架构详解

### 1. 滑动窗口编码器

处理长汇编序列的核心组件：

```
输入序列 (长度 L)
    ↓
切分为 K 个重叠窗口
    ↓
每个窗口独立编码
    ↓
重叠区域特征平均
    ↓
全局特征矩阵 H_global
```

窗口数量计算：$K = \lceil (L-W)/S \rceil + 1$

### 2. Q-Former 注意力桥接

将冗长特征压缩为固定长度：

```
可学习查询 Q ∈ R^{64×768}
    ↓
自注意力 (查询间交互)
    ↓
交叉注意力 (从 H_global 提取信息)
    ↓
线性投影 → LLM 维度
    ↓
软提示符 P ∈ R^{64×4096}
```

### 3. 蒸馏式思维链

训练时使用 GPT-4 生成的分析作为监督信号：

```
教师模型 (GPT-4 + 源代码)
    ↓
生成三阶段分析：
1. 架构与约定分析
2. 数据流与特征提取
3. 功能归纳
    ↓
学生模型 (Binbridge)
仅输入汇编代码，学习生成相似分析
```

### 4. 混合损失函数

$$\mathcal{L} = \lambda_{analysis} \cdot \mathcal{L}_{analysis} + \lambda_{name} \cdot \mathcal{L}_{name}$$

- $\mathcal{L}_{analysis}$: 分析部分的交叉熵损失
- $\mathcal{L}_{name}$: 函数名部分的交叉熵损失
- 默认权重: $\lambda_{analysis}=0.3$, $\lambda_{name}=0.7$

## 📊 性能指标

| 指标 | 值 |
|------|-----|
| 精确匹配率 | 45.2% |
| Token 准确率 | 78.6% |
| BLEU-4 | 0.423 |
| 推理速度 | ~50 samples/sec |

*注：以上为示例数据，实际性能取决于训练数据质量和规模*

## ⚙️ 配置说明

### 编码器配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_seq_length` | 512 | 预训练模型最大长度 |
| `window_size` | 512 | 滑动窗口大小 |
| `stride` | 256 | 滑动步长 |
| `freeze_encoder` | True | 是否冻结编码器 |

### Q-Former 配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `num_query_tokens` | 64 | 查询向量数量 |
| `num_hidden_layers` | 6 | Transformer 层数 |
| `cross_attention_freq` | 2 | 交叉注意力频率 |

### 训练配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `learning_rate` | 2e-4 | 学习率 |
| `batch_size` | 4 | 批大小 |
| `gradient_accumulation_steps` | 4 | 梯度累积步数 |
| `qlora_r` | 64 | LoRA 秩 |

## 📝 数据量建议

根据实战经验：

| 阶段 | 数据量 | 效果 |
|------|--------|------|
| 验证原型 (MVP) | 500-1,000 条 | 模型掌握 XML 格式 |
| 具备实战能力 | 3,000-10,000 条 | 思维链蒸馏效果显现 |
| 生产级/SOTA | 50,000+ 条 | 覆盖冷门架构和优化级别 |

**策略建议**：先用 GPT-4 生成 1,000 条高质量的"教科书级"分析，用这 1k 条去微调，效果可能已经惊人地好了。

## 🔧 常见问题

**Q: 显存不足怎么办？**

A: 尝试以下方法：
- 减小 `batch_size`
- 增大 `gradient_accumulation_steps`
- 确保启用了 4-bit 量化 (`load_in_4bit=True`)
- 减少 `num_query_tokens`

**Q: 如何处理非 x86 架构？**

A: 在训练数据中包含 ARM、MIPS 等架构的样本，模型会自动学习架构特定的模式。

**Q: 推理时需要保留分析过程吗？**

A: 不需要。推理时分析过程作为"思维暂存区"对逻辑连贯性至关重要，但最终只返回函数名。

## 📄 许可证

MIT License

## 🙏 致谢

- BLIP-2 架构启发了 Q-Former 设计
- CLAP-ASM 提供了预训练的汇编编码器
- Llama 3.2 作为基础语言模型

## 📚 引用

如果您使用了 Binbridge，请引用：

```bibtex
@article{binbridge2024,
  title={Binbridge: Multimodal LLM for Binary Code Function Naming},
  author={Your Name},
  year={2024}
}
```
